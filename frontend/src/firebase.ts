/**
 * Firebase is optional; the app uses Supabase. Install `firebase` and configure VITE_FIREBASE_* to enable.
 * Stub keeps `tsc` happy when the package is not installed.
 */
export const auth = null;
export const db = null;
export default null;
